# Math Challenge — Infraestructura Cloudflare

> **Inventario canónico de objetos.** Todo objeto que Math Challenge crea en la
> cuenta de Cloudflare lleva el prefijo `math-challenge-` para distinguirlo de
> los de IOS y de IMP, que viven en la misma cuenta.
>
> **Estado: 5 de 28 objetos creados.** El resto de esta lista es lo que se va a
> crear, no lo que está creado. Conforme se creen, se anota el ID real y la fecha
> en la bitácora de abajo, según la regla de `CLAUDE.md` § Cloudflare.
>
> Bilingüe (EN/ES) porque la columna de propósito la lee tanto quien opera la
> cuenta como quien escribe el código.
>
> **Origen:** derivado de `research/2026-07-31-mc-32-cloudflare-architecture.md`,
> donde cada límite y cada precio está citado contra una página de documentación
> de Cloudflare efectivamente descargada. Si necesitas el *porqué* de una
> elección, está allá; aquí está el *qué*.

## Regla de nombres

```
math-challenge-<qué-es>[-<tipo>]
```

Los bindings van en `UPPER_SNAKE_CASE`. El prefijo nunca se omite, ni siquiera en
entornos de prueba — ahí se sufija: `math-challenge-db-dev`.

## Decisiones estructurales que explican esta lista

1. **Los intentos NO van a D1.** D1 topa en 10 GB por base y sería la primera
   pared que golpeamos. Los millones de intentos crudos van a Analytics Engine.
2. **Un Durable Object por liga y por salón**, no uno global. Cada liga tiene
   ~30 miembros y su propio estado en vivo por WebSocket.
3. **Un Durable Object por niño** para el modelo adaptativo, porque la selección
   del siguiente ítem necesita estado consistente y de baja latencia.
4. **KV guarda instantáneas del tablero**, nunca escrituras por intento — KV
   admite una escritura por segundo por llave.
5. **La inferencia corre sobre Workers AI, dentro del Worker** (D-035, y la
   ampliación del dueño el mismo día: «solo vamos a trabajar con Cloudflare, es
   una decisión tomada»). No queda ningún camino a la API de Claude.

   Esta línea decía «AI Gateway va delante de Claude siempre, para caché, límite
   de gasto por perfil y ruteo de modelo», y era falsa en las tres cosas desde
   D-035. Lo que vale hoy, corregido en F6 #136:

   - **El ruteo por banda** lo hace el Worker (`packages/tutor/src/en-vivo.ts`),
     con `gpt-oss-120b` abajo y `kimi-k2.6` arriba. Es el punto 3 de D-004 con
     otros modelos.
   - **El tope de gasto por perfil y por día** lo hace el **Durable Object**, no
     el Gateway. El plan de F6 §5.1 enmienda D-015 y explica por qué: el objeto
     decide ANTES de gastar y puede degradar con criterio pedagógico —servir la
     explicación pregenerada revisada por humano—, y eso el Gateway no lo sabe
     hacer. Puede devolver 429 o cambiar a un modelo más barato, y lo segundo es
     justo lo que D-035 prohíbe para la banda Pro.
   - **El Gateway se queda como red de seguridad en DÓLARES**, y con una
     advertencia medible: si su base de precios no cubre los modelos `@cf/`, el
     tope en dólares no dispara nunca. Se comprueba llamando, no leyendo.
   - **La caché del Gateway va apagada**: solo empata peticiones idénticas, sin
     caché semántico, y con un ítem distinto por petición la tasa de acierto es
     ~0. Lo que sí se cachea es el **prefijo de sistema**, por
     `larry|<locale>|<banda>` — catorce llaves en el MVP, treinta y cinco en la
     escalera completa, y **jamás por perfil del niño**.

## Inventario de objetos / Resource inventory

Every object is prefixed `math-challenge-` as required. Binding names use `UPPER_SNAKE_CASE`.

| Name | Type | Purpose (EN) | Propósito (ES) | Binding |
|---|---|---|---|---|
| `math-challenge-web` | Worker (Astro, Static Assets) | Public PWA frontend + BFF routes | Frontend PWA público + rutas BFF | n/a (entry Worker) |
| `math-challenge-ingest` | Worker | Validates and ingests attempt submissions; writes telemetry, enqueues scoring | Valida e ingiere envíos de intentos; escribe telemetría, encola calificación | `INGEST` (service binding desde web) |
| `math-challenge-tutor` | Worker | ⚠️ NOT CREATED, and F6 did not need it. Larry's live path lives inside `math-challenge-web` (`/api/larry`) because the sealed envelope, the profile session and the spend meter are already there; a second Worker would move the prompt away from the only place that can seal it. The row stays as a reminder that it is not pending — it is unnecessary | ⚠️ NO CREADO, y F6 no lo necesitó. El camino en vivo de Larry vive dentro de `math-challenge-web` (`/api/larry`): el sobre sellado, la sesión del perfil y el medidor de gasto ya están ahí, y un segundo Worker alejaría el prompt del único sitio que puede sellarlo | n/a |
| `math-challenge-leaderboard-cron` | Worker (Cron Trigger) | Triggers the periodic leaderboard rollup Workflow | Dispara el Workflow periódico de recálculo de leaderboard | n/a |
| `math-challenge-db` | D1 database | System of record: users, children, classrooms, leagues, content metadata, consent | Registro maestro: usuarios, niños, salones, ligas, metadatos de contenido, consentimiento | `DB` |
| `math-challenge-league-do` | Durable Object class (SQLite) | Live state + WebSocket broadcast for one league of ~30 | Estado en vivo + difusión WebSocket de una liga de ~30 | `LEAGUE_DO` |
| `math-challenge-classroom-do` | Durable Object class (SQLite) | Live state for one classroom's roster and in-class standings | Estado en vivo del roster y clasificación de un salón | `CLASSROOM_DO` |
| `math-challenge-db` · migración `0003_accounts_onboarding.sql` | D1 schema | Accounts and onboarding: derived signup columns, consent catalog with abort trigger, household devices, teacher verification, contextual marks | Cuentas y onboarding: columnas derivadas del registro, catálogo de consentimiento con trigger de aborto, dispositivos de la casa, verificación de maestro, marcas contextuales | `DB` |
| `math-challenge-db` · migración `0004_consent_governance.sql` | D1 schema | One governed consent path: consent_records frozen by trigger, consent_version and current_version added, cookie mc_h corrected to D1 | Un solo camino de consentimiento con gobierno: consent_records congelada por trigger, consent_version y current_version añadidas, cookie mc_h corregida a D1 | `DB` |
| `math-challenge-db` · migración `0005_group_owner_identity.sql` | D1 schema | teacher_verifications renamed to group_owner_identity: this product does not verify anyone is a teacher. `assurance='declared'` means the person wrote it and nobody checked, and that is what the parent is shown | teacher_verifications renombrada a group_owner_identity: este producto no verifica que nadie sea maestro. `assurance='declared'` significa que lo escribió esa persona y nadie lo comprobó, y eso es lo que se le muestra al padre | `DB` |
| `math-challenge-db` · migración `0006_solo_el_anio.sql` | D1 schema | birth_month removed from child_profiles: the band derives from the YEAR and the month feeds no product decision. Data minimisation under D-013, declared with the narrow `migration-safety-minimizacion` marker that requires naming the column | birth_month retirada de child_profiles: la banda se deriva del AÑO y el mes no alimenta ninguna decisión del producto. Minimización de datos por D-013, declarada con el marcador estrecho `migration-safety-minimizacion` que exige nombrar la columna | `DB` |
| `math-challenge-db` · migración `0007_racha_y_xp.sql` | D1 schema | Two new add-only tables: child_streak (one row per learner, local-day streak, earned shields capped at 2, family pause) and xp_totals (lifetime XP, no period, no theme_band, and deliberately NO rango column — the rank derives from total_xp on read). Neither has a price, currency, coupon or free-text column, and neither is a per-attempt table | Dos tablas nuevas que solo agregan: child_streak (una fila por aprendiz, racha por día local, escudos ganados con tope 2, pausa familiar) y xp_totals (XP de por vida, sin period, sin theme_band, y deliberadamente SIN columna rango — el rango se deriva de total_xp al leer). Ninguna tiene columna de precio, moneda, cupón ni texto libre, y ninguna es una tabla por intento | `DB` |
| `math-challenge-db` · migración `0008_escudos_por_racha.sql` | D1 schema | Add-only column child_streak.shields_earned_this_streak: the cap of 2 shields is PER STREAK, not every seven days (D-079). Without it the bank refilled forever and, past day 14, skipping one day in seven cost almost nothing. Written as a new migration and not as an edit to 0007 because D1 tracks migrations BY FILENAME — an already-applied 0007 never runs again | Columna que solo agrega, child_streak.shields_earned_this_streak: el tope de 2 escudos es POR RACHA, no cada siete días (D-079). Sin ella el banco se reponía para siempre y, pasado el día 14, saltarse un día de cada siete no costaba casi nada. Va como migración nueva y no como edición de la 0007 porque D1 lleva el control POR NOMBRE DE ARCHIVO — una 0007 ya aplicada no vuelve a correr | `DB` |
| `math-challenge-db` · migración `0012_ligas_tablero_duelo.sql` | D1 schema | Add-only: `league_cohort` and `league_membership` (cohorts split by band AND by participant type, polymorphic membership), `league_duel` (frozen item set, 48 h window, no presence column at all), `score_totals_adulto` (the adult learner had nowhere to accumulate: `score_totals` references `child_profiles`), `users.alias`/`alias_locale`, and two consent codes — `LEAGUE` and `DUEL`. No price, currency, coupon, free-text or presence column exists in any of them | Solo agrega: `league_cohort` y `league_membership` (cohortes separadas por banda Y por tipo de participante, membresía polimórfica), `league_duel` (set de ítems congelado, ventana de 48 h, ni una sola columna de presencia), `score_totals_adulto` (el adulto aprendiz no tenía dónde acumular: `score_totals` referencia `child_profiles`), `users.alias`/`alias_locale`, y dos códigos de consentimiento — `LEAGUE` y `DUEL`. Ninguna tiene columna de precio, moneda, cupón, texto libre ni presencia | `DB` |
| `math-challenge-db` · migración `0014_push_recordatorio_padre.sql` | D1 schema | Add-only: `push_subscription` (one Web Push endpoint per ADULT device — `user_id` → `users`, `endpoint` UNIQUE, `p256dh`/`auth`; deliberately NO `child_profile_id` column anywhere: the recipient of the reminder is always the parent, and `audits/recordatorio-sin-culpa.mjs` blocks the commit that adds one — issue #207 criterion #1) and `push_recordatorio` (per-HOUSEHOLD state: `last_sent_local_date` for the hard cap of one push per day per home, and `silenciado_at`, a one-way permanent mute that no route clears — D-026). Zero new Cloudflare objects: both tables live in `math-challenge-db`. The VAPID sender keys are NOT in the schema or the repo: private via `wrangler secret put`, public as a var; without them the cron degrades silently | Solo agrega: `push_subscription` (un endpoint Web Push por aparato del ADULTO — `user_id` → `users`, `endpoint` UNIQUE, `p256dh`/`auth`; deliberadamente SIN columna `child_profile_id` en ninguna parte: el destinatario del recordatorio es siempre el padre, y `audits/recordatorio-sin-culpa.mjs` bloquea el commit que la añada — issue #207 criterio #1) y `push_recordatorio` (estado por HOGAR: `last_sent_local_date` para el tope duro de un push al día por hogar, y `silenciado_at`, un silencio permanente de una sola vía que ninguna ruta limpia — D-026). Cero objetos nuevos de Cloudflare: las dos tablas viven en `math-challenge-db`. Las claves VAPID del remitente NO están en el esquema ni en el repo: la privada con `wrangler secret put`, la pública como var; sin ellas el cron degrada en silencio | `DB` |
| `math-challenge-db` · migración `0009_misiones_diarias.sql` | D1 schema | Add-only table mission_daily_summary: ONE row per (learner, local day, mission type) — at most three per person per day, never one per attempt (mc-32 risk #1). Polymorphic like child_streak: a child profile or an adult learner, never both. mission_type is TEXT with a CHECK closed to the ten catalogue types, cross-checked against `packages/motor/src/misiones.ts` by `audits/mision-recompensa-deterministica.mjs`. No price, currency, coupon or plan column (red line #4: practising is never charged for), no probability or rarity column (red line #5), no free-text column (red line #3), and no board-points column (#225). KINDER writes nothing here: its "daily mission" is the day's HISTORIA challenge in the Savannah (D-019) | Tabla que solo agrega, mission_daily_summary: UNA fila por (aprendiz, día local, tipo de misión) — como mucho tres por persona y por día, jamás una por intento (mc-32 riesgo #1). Polimórfica como child_streak: un perfil de niño o un adulto aprendiz, nunca los dos. mission_type es TEXT con un CHECK cerrado a los diez tipos del catálogo, cruzado contra `packages/motor/src/misiones.ts` por `audits/mision-recompensa-deterministica.mjs`. Sin columna de precio, moneda, cupón ni plan (línea roja #4: nunca se cobra por practicar), sin columna de probabilidad ni rareza (línea roja #5), sin columna de texto libre (línea roja #3) y sin columna de puntos de tablero (#225). KINDER no escribe aquí: su «misión diaria» es el reto HISTORIA del día en la Sabana (D-019) | `DB` |
| `math-challenge-db` · migración `0010_mapa_companero.sql` | D1 schema | New add-only table companion_state: two preferences of the person (visible, accessory_ids) for the map companion, which is Larry himself with earned accessories (D-080). Deliberately NO hunger, happiness, health, energy, mood or last_fed_at column — mc-43 §6 shows Tamagotchi's retention mechanism and its guilt mechanism are the same mechanism, so the decay state is impossible by construction rather than forbidden by rule. No price, currency or drop-rate column either (red line #5). The MAP itself has no table: it is a read layer over skill_state, EstadoHistoria, xp_totals and child_streak (#231) | Tabla nueva que solo agrega, companion_state: dos preferencias de la persona (visible, accessory_ids) para el compañero del mapa, que es Larry mismo con accesorios ganados (D-080). Deliberadamente SIN columna de hambre, felicidad, salud, energía, humor ni última alimentación — `mc-43` §6 documenta que el mecanismo de retención de Tamagotchi y su mecanismo de culpa son el mismo, así que el decaimiento es imposible por construcción y no prohibido por regla. Tampoco hay columna de precio, moneda ni probabilidad (línea roja #5). El MAPA no tiene tabla: es una capa de lectura sobre skill_state, EstadoHistoria, xp_totals y child_streak (#231) | `DB` |
| `math-challenge-db` · migración `0013_dias_jugados_sendero.sql` | D1 schema | Add-only column child_streak.days_played_total: the accumulated count of days PLAYED that backs the KINDER streak path (#205) — one step per day played, and the path never goes back when current_streak resets (mc-43 §6). Neither current_streak (drops to 1) nor max_streak (best single streak) can express it. Incremented only inside conDia() in racha.ts, the single funnel where a day counts, and the motivo does not enter: a day completed by screen-time cut counts as a played day too (red line #6). Existing streaks start at 0 — pre-migration days cannot be reconstructed and painting current_streak as if it were the total would be inventing data | Columna que solo agrega, child_streak.days_played_total: el acumulado de días JUGADOS que sostiene el sendero de racha de KINDER (#205) — un paso por día jugado, y el sendero jamás retrocede cuando current_streak se reinicia (mc-43 §6). Ni current_streak (baja a 1) ni max_streak (la mejor racha) pueden expresarlo. Lo incrementa solo conDia() de racha.ts, el único embudo por el que un día cuenta, y el motivo no entra: un día cumplido por corte de límite de pantalla también suma paso (línea roja #6). Las rachas existentes arrancan en 0 — los días anteriores a la migración no se pueden reconstruir, y pintar current_streak como si fuera el acumulado sería inventar un dato | `DB` |
| `math-challenge-db` · migración `0017_grupos_infantiles.sql` | D1 schema | F9 · Groups of children (#379-#387): five add-only tables — `school` (verified ONCE as an institution, D-086; review queue staffed by the owner alone, D-089; lax universal document standard, D-090), `school_teacher` (invite/revoke, `revoked_at` never DELETE), `child_group` (ONE table for classroom and parents' club, `origen_tipo` column, 6-char join code with no ambiguous chars, hard `max_size` cap of 35), `child_group_membership` (one row per REQUEST, never deleted — the membership IS the consent, `decided_by`/`decided_at`; `leaderboard_opt_in` defaults 0, set by the parent only, D-087), `child_group_report` (closed five-value `reason_code` CHECK, NO screenshot — state is rebuilt from D1) — plus the 12-step rebuild of `group_owner_identity` adding the fourth `assurance` value `school_verified`, and the triggers that enforce the seat cap on approval and sync `assurance` with school verification/revocation in the SAME transaction. Deliberately NO free-text column between adult and child anywhere (red line #3), no price/currency/chance column (red lines #4-#5), no per-attempt table (mc-32 risk #1). Zero new Cloudflare objects: all tables live in `math-challenge-db` | F9 · Grupos infantiles (#379-#387): cinco tablas que solo agregan — `school` (verificada UNA vez como institución, D-086; la cola de revisión la atiende el dueño solo, D-089; estándar de documento laxo y universal, D-090), `school_teacher` (invita/revoca, `revoked_at` jamás DELETE), `child_group` (UNA tabla para salón y club de papás, columna `origen_tipo`, código de unión de 6 caracteres sin ambiguos, tope duro `max_size` de 35), `child_group_membership` (una fila por SOLICITUD, nunca borrada — la membresía ES el consentimiento, `decided_by`/`decided_at`; `leaderboard_opt_in` default 0 y solo lo pone el padre, D-087), `child_group_report` (`reason_code` con CHECK cerrado de cinco valores, SIN captura — el estado se reconstruye de D1) — más la reconstrucción de 12 pasos de `group_owner_identity` que añade el cuarto valor de `assurance`, `school_verified`, y los triggers que hacen cumplir el cupo al aprobar y sincronizan `assurance` con la verificación/revocación de la escuela EN LA MISMA TRANSACCIÓN. Deliberadamente SIN columna de texto libre entre adulto y niño (línea roja #3), sin precio, moneda ni azar (líneas rojas #4-#5), sin tabla por intento (mc-32 riesgo #1). Cero objetos nuevos de Cloudflare: todas las tablas viven en `math-challenge-db` | `DB` |
| `math-challenge-sesion-reto-do` | Durable Object class (SQLite) | One challenge session: two server timestamps, idempotent scoring by (session, order), safe cut point | Una sesión de reto: los dos sellos del servidor, puntuación idempotente por (sesión, orden), punto seguro de corte | `SESION_RETO_DO` |
| `math-challenge-db` · migración `0015_cosmeticos_kinder_v1.sql` | D1 schema | Three add-only tables — `cosmetic_catalog` (23 KINDER v1 rows: 3 initial avatar pieces + 1 welcome + 14 tied to Savanna skill mastery, 6 of them published WITHOUT art until F5 closes those skills, + 3 initial frames + 2 milestone frames), `cosmetic_unlock_rules` (deterministic, mirrors ReglaDeDesbloqueo from packages/motor/src/cosmeticos.ts), `child_cosmetics_unlocked` (PK (child_profile_id, cosmetic_id), ON DELETE CASCADE) — plus index idx_skill_mastered on skill_state (child_profile_id, mastered_at) for the counting rule. Deliberately NO price, currency, probability or rarity column anywhere (red line #5, D-014): the absence is structural. Names and conditions are i18n KEYS (apps/web/src/i18n/cosmeticos/), never raw text (D-022); the condition text is parent-panel only (mc-43 §8). The schema is created HERE and not in an earlier migration because issue #253 was closed as done but its CREATE TABLEs never landed in main — noted in the migration file itself. Zero new Cloudflare objects | Tres tablas que solo agregan — `cosmetic_catalog` (23 filas del v1 de KINDER: 3 piezas iniciales + 1 de bienvenida + 14 ligadas a maestría de habilidad de la Sabana, 6 de ellas publicadas SIN arte hasta que F5 cierre esas habilidades, + 3 marcos iniciales + 2 marcos de hito), `cosmetic_unlock_rules` (determinista, espejo de ReglaDeDesbloqueo de packages/motor/src/cosmeticos.ts), `child_cosmetics_unlocked` (PK (child_profile_id, cosmetic_id), ON DELETE CASCADE) — más el índice idx_skill_mastered sobre skill_state (child_profile_id, mastered_at) para la regla de conteo. Deliberadamente SIN columna de precio, moneda, probabilidad ni rareza (línea roja #5, D-014): la ausencia es estructural. Nombres y condiciones son CLAVES i18n (apps/web/src/i18n/cosmeticos/), jamás texto crudo (D-022); la condición solo se muestra al padre (mc-43 §8). El esquema se crea AQUÍ y no en una migración anterior porque la issue #253 se cerró como hecha pero sus CREATE TABLE nunca aterrizaron en main — anotado en el propio archivo de la migración. Cero objetos nuevos de Cloudflare | `DB` |
| `math-challenge-db` · migración `0016_banco_items_primaria.sql` | D1 schema | Add-only table `item_bank` (one row per item, the item stored as STRUCTURE per master-plan §9 — `item_json` holds the `Item` from packages/motor/src/item.ts, never pre-rendered text) plus its reading index `idx_item_bank_lectura (banda, habilidad, nivel)`. `hasta_nivel` is a nullable service ceiling so a scaffold model can switch off by level (Kalyuga's expertise reversal, #354). Deliberately NO attempt data (mc-32 risk #1 — attempts stay out of D1), NO price/probability columns (red lines #4/#5), NO child reference. Seeding is NOT in the migration: `scripts/sembrar-banco-primaria.mjs` generates it from the parametric templates with INSERT OR IGNORE, so a hand-curated row is never overwritten by a re-seed (that editability is the whole point of D-072). Zero new Cloudflare objects: the table lives in `math-challenge-db` | Tabla que solo agrega: `item_bank` (una fila por ítem, el ítem como ESTRUCTURA según el plan §9 — `item_json` guarda el `Item` de packages/motor/src/item.ts, jamás texto ya formado) más su índice de lectura `idx_item_bank_lectura (banda, habilidad, nivel)`. `hasta_nivel` es un techo de servicio anulable para que un modelo de andamiaje se apague por nivel (reversión de la pericia de Kalyuga, #354). Deliberadamente SIN intentos (mc-32 riesgo #1 — los intentos siguen fuera de D1), SIN precio ni probabilidad (líneas rojas #4/#5), SIN referencia a ningún niño. La siembra NO va en la migración: la genera `scripts/sembrar-banco-primaria.mjs` desde las plantillas paramétricas con INSERT OR IGNORE, así que una fila curada a mano nunca la pisa una re-siembra (esa editabilidad es la razón de ser de D-072). Cero objetos nuevos de Cloudflare: la tabla vive en `math-challenge-db` | `DB` |
| `math-challenge-learner-do` | Durable Object class (SQLite) | Per-child adaptive learner model (mastery estimates, item selection state) | Modelo de aprendizaje adaptativo por niño | `LEARNER_DO` |
| `math-challenge-db` · migración `0018_child_diagnostic_notes.sql` | D1 schema | Add-only table `child_diagnostic_notes`: the system notes the parent panel renders (#278, F8) — `cause_code` with a CHECK closed to two values (`HABILIDAD_PAUSADA_LATERAL` from F6's lateral pedagogical descent, `PATRON_INUSUAL_PARA_EDAD` from D-020), never a free-text column (red line #3; the rendered text is an authored per-locale template in `apps/web/src/i18n/padre/`), never a DELETE path (notes are only marked `seen_at`, #283), ON DELETE CASCADE like every child table (mc-32 risk #7). The writers (F4/F6) do not exist yet: this migration fixes the table CONTRACT so those phases implement it without reopening schema. NO price/plan/consent column — the panel is free for every parent (D-057) and `SCREEN_TIME`/`DATA_RETENTION` (D-051) already cover the data category. Zero new Cloudflare objects | Tabla que solo agrega: `child_diagnostic_notes`: las notas del sistema que renderiza el panel del padre (#278, F8) — `cause_code` con CHECK cerrado a dos valores (`HABILIDAD_PAUSADA_LATERAL`, el descenso pedagógico lateral de F6; `PATRON_INUSUAL_PARA_EDAD`, D-020), jamás una columna de texto libre (línea roja #3; el texto renderizado es una plantilla autorada por locale en `apps/web/src/i18n/padre/`), jamás un camino de DELETE (las notas solo se marcan `seen_at`, #283), ON DELETE CASCADE como toda tabla de niño (mc-32 riesgo #7). Los escritores (F4/F6) no existen todavía: esta migración fija el CONTRATO de la tabla para que esas fases lo implementen sin reabrir esquema. SIN columna de precio/plan/consentimiento — el panel es gratis para todo padre (D-057) y `SCREEN_TIME`/`DATA_RETENTION` (D-051) ya cubren la categoría de dato. Cero objetos nuevos de Cloudflare | `DB` |
| `math-challenge-ratelimiter-do` | Durable Object class (SQLite) | Sharded rate limiting (login attempts, tutor calls, signup) | Limitación de tasa fragmentada (inicios de sesión, llamadas al tutor, registro) | `RATE_LIMITER_DO` |
| `math-challenge-leaderboard-kv` | KV namespace | Precomputed global/grade-band leaderboard snapshots | Instantáneas precalculadas del leaderboard global/por-grado | `LEADERBOARD_KV` |
| `math-challenge-config-kv` | KV namespace | Feature flags and content-catalog cache | Feature flags y caché del catálogo de contenido | `CONFIG_KV` |
| `math-challenge-session-kv` | KV namespace | Short-lived auth/session tokens | Tokens de sesión/autenticación de corta duración | `SESSION_KV` |
| `math-challenge-media` | R2 bucket | Item images, audio, illustrations | Imágenes, audio e ilustraciones de los reactivos | `MEDIA_BUCKET` |
| `math-challenge-exports` | R2 bucket | Cold archive of aged-out attempts; COPPA/GDPR data-subject exports | Archivo frío de intentos vencidos; exportaciones para solicitudes COPPA/GDPR | `EXPORTS_BUCKET` |
| `math-challenge-scoring-queue` | Queue | Async scoring + learner-model update jobs | Trabajos asíncronos de calificación y actualización del modelo de aprendizaje | `SCORING_QUEUE` |
| `math-challenge-scoring-dlq` | Queue (dead-letter) | Failed scoring jobs after max retries | Trabajos de calificación fallidos tras reintentos máximos | `SCORING_DLQ` |
| `math-challenge-reports-queue` | Queue | One message per household due for its email report (F8 #289): the hourly cron of `math-challenge-web` only enumerates and enqueues, never renders inline; the consumer builds the report, renders the parent-locale template and sends with the `EMAIL` binding. Batch of 10, 3 retries, then DLQ | Un mensaje por hogar al que le toca su reporte por correo (F8 #289): el cron horario de `math-challenge-web` solo enumera y encola, nunca renderiza en línea; el consumidor construye el reporte, renderiza la plantilla del locale del padre y envía con el binding `EMAIL`. Lote de 10, 3 reintentos, y a la DLQ | `REPORTES_QUEUE` |
| `math-challenge-reports-dlq` | Queue (dead-letter) | Failed report-send jobs after max retries. A `E_RECIPIENT_SUPPRESSED` address does NOT land here by retry: the consumer detects it, switches the household cadence to OFF and acks — the sender reputation is already "At Risk" and retrying a bouncing address is how it sinks | Trabajos de envío de reporte fallidos tras los reintentos máximos. Una dirección `E_RECIPIENT_SUPPRESSED` NO llega aquí por reintento: el consumidor la detecta, apaga la cadencia del hogar y confirma — la reputación del remitente ya está «At Risk» y reintentar contra un rebote es cómo se hunde | n/a (dead-letter de `math-challenge-reports-queue`) |
| `math-challenge-db` · migración `0019_reportes_correo.sql` | D1 schema | F8 · Email reports to the parent (#287): two add-only tables, ONE row per entity, never one per send — `parent_report_settings` (the PARENT's preference: `cadence` WEEKLY by owner's default / MONTHLY / OFF, `send_hour_local` 7–20 — the mc-19 rec. #13 quiet window applied to email, in `users.timezone` —, `last_sent_at` written only AFTER the confirmed send, `unsubscribe_token` opaque with `nuevoToken()` entropy, never derived from `user_id`, and `unsubscribed_at`, a one-way seal no route clears — D-026) and `child_report_state` (the per-child snapshot the period is measured against: `last_reported_at`, `last_score_all_time`, `last_xp_total` NULLable because "no data" and "zero" are different claims). Both `ON DELETE CASCADE` (erasure is the cascade, mc-32 risk #7), no per-attempt field (mc-32 risk #1), NO new `consent_code` — cadence is delivery of already-consented data, D-051. Declares `migration-safety-reserva: 0018` for the parallel panel front | F8 · Reportes por correo al padre (#287): dos tablas que solo agregan, UNA fila por entidad, jamás una por envío — `parent_report_settings` (la preferencia del PADRE: `cadence` WEEKLY por default del dueño / MONTHLY / OFF, `send_hour_local` 7–20 —la ventana de silencio de mc-19 rec. #13 aplicada al correo, en `users.timezone`—, `last_sent_at` escrito solo TRAS el envío confirmado, `unsubscribe_token` opaco con la entropía de `nuevoToken()`, nunca derivado de `user_id`, y `unsubscribed_at`, un sello de una vía que ninguna ruta limpia — D-026) y `child_report_state` (el snapshot por hijo contra el que se mide el periodo: `last_reported_at`, `last_score_all_time`, `last_xp_total` anulable porque «no hay dato» y «cero» son afirmaciones distintas). Ambas `ON DELETE CASCADE` (el borrado es el cascade, mc-32 riesgo #7), sin campo por intento (mc-32 riesgo #1), SIN `consent_code` nuevo — la cadencia es entrega de datos ya consentidos, D-051. Declara `migration-safety-reserva: 0018` para el frente del panel, en paralelo | `DB` |
| `math-challenge-db` · migración `0020_cosmeticos_arte_restante.sql` | D1 schema | Data-only: six UPDATEs fill `arte_avif_url`/`arte_webp_url` on the six `cosmetic_catalog` rows the 0015 published with NULL art (#255 — K05, K06, K08, K09, K13, K14, whose skills F5 has since closed), each guarded by `AND arte_avif_url IS NULL` so a row WITH art is never overwritten from here — the 15 approved pieces are structurally untouchable by this migration. The art was generated with `scripts/gen-cosmeticos.mjs` (same style, format and size as the 15 approved: AVIF + WebP at 512 px in `/cosmeticos/`) and every image was LOOKED AT before being accepted — the first cosmetic pass produced 5 of 15 bad ones. The two milestone frames stay art-less on purpose (their thresholds are unreachable today). Zero new Cloudflare objects | Solo datos: seis UPDATE rellenan `arte_avif_url`/`arte_webp_url` en las seis filas de `cosmetic_catalog` que la 0015 publicó con arte NULL (#255 — K05, K06, K08, K09, K13, K14, cuyas habilidades F5 ya cerró), cada una con el seguro `AND arte_avif_url IS NULL` para que una fila CON arte jamás se pise desde aquí — las 15 piezas aprobadas son estructuralmente intocables para esta migración. El arte se generó con `scripts/gen-cosmeticos.mjs` (mismo estilo, formato y tamaño que las 15 aprobadas: AVIF + WebP a 512 px en `/cosmeticos/`) y cada imagen se MIRÓ antes de aceptarse — la primera pasada de cosméticos sacó 5 de 15 malas. Los dos marcos de hito siguen sin arte a propósito (sus umbrales hoy son inalcanzables). Cero objetos nuevos de Cloudflare | `DB` |
| `math-challenge-db` · migración `0021_alias_unico_por_padre.sql` | D1 schema | F2 fix (#259): the child alias gets its unique index FOR REAL. `perfil-nuevo.ts` caught `/UNIQUE/i` to retry with another alias while its comment swore the index existed "since migration 0003" — false; the catch was dead code (37% chance of at least one collision with 1,000 children in es-MX, re-runnable math in the issue). The index actually arrived in 0006; this migration does what 0006 could not: an EXPLICIT, deterministic, idempotent dedup pass (keeps the OLDEST profile of each (parent, alias) group, renames the rest with a `-XXXXXX` suffix derived from their own id, NEVER deletes a profile — erasure has its own runbook, mc-32 risk #7) and then re-asserts `idx_alias_por_padre` UNIQUE (parent_user_id, alias) WHERE deleted_at IS NULL with IF NOT EXISTS, so it is a no-op where 0006 already applied and a guarantee where it silently didn't. Per-parent, not global: two families may share an alias; two children of the SAME parent may not. Zero new Cloudflare objects | Fix de F2 (#259): el alias del niño recibe su índice único DE VERDAD. `perfil-nuevo.ts` capturaba `/UNIQUE/i` para reintentar con otro alias mientras su comentario juraba que el índice existía «desde la migración 0003» — falso; el catch era código muerto (37% de probabilidad de al menos un choque con 1,000 niños en es-MX, cálculo re-ejecutable en el issue). El índice llegó en realidad en la 0006; esta migración hace lo que la 0006 no podía: una deduplicación EXPLÍCITA, determinista e idempotente (CONSERVA el perfil más antiguo de cada grupo (padre, alias), RENOMBRA a los demás con un sufijo `-XXXXXX` derivado de su propio id, NUNCA borra un perfil — el borrado tiene su propio runbook, mc-32 riesgo #7) y después reafirma `idx_alias_por_padre` UNIQUE (parent_user_id, alias) WHERE deleted_at IS NULL con IF NOT EXISTS, así que es un no-op donde la 0006 ya aplicó y una garantía donde no llegó en silencio. Por padre, no global: dos familias pueden compartir alias; dos hijos del MISMO padre, no. Cero objetos nuevos de Cloudflare | `DB` |
| `math-challenge-ai-explain-queue` | Queue | Async AI-explanation generation requests | Solicitudes asíncronas de generación de explicaciones de IA | `AI_EXPLAIN_QUEUE` |
| `math-challenge-ai-explain-dlq` | Queue (dead-letter) | Failed explanation jobs after max retries | Trabajos de explicación fallidos tras reintentos máximos | `AI_EXPLAIN_DLQ` |
| `math-challenge-leaderboard-rollup-workflow` | Workflow | Periodic global/grade-band leaderboard computation | Cálculo periódico del leaderboard global/por-grado | `LEADERBOARD_WORKFLOW` |
| `math-challenge-onboarding-workflow` | Workflow | Multi-step account + child-profile + consent setup | Configuración multi-paso de cuenta + perfil de niño + consentimiento | `ONBOARDING_WORKFLOW` |
| `math-challenge-explanations-index` | Vectorize index | Multilingual RAG index over curated hints/explanations | Índice RAG multilingüe sobre pistas/explicaciones curadas | `EXPLANATIONS_INDEX` |
| `math-challenge-tutor-gateway` | AI Gateway | ⚠️ NOT CREATED YET. Dollar-denominated safety net in front of Workers AI — never in front of Claude, that path was removed by D-035. The per-profile/day cap does NOT depend on it: it lives in the Durable Object (F6 plan §5.1, amends D-015). Zero Data Retention in production: counts and costs, never prompts — the prompt carries what a minor answered. Cache OFF (see structural decision 5) | ⚠️ TODAVÍA NO CREADO. Red de seguridad en dólares delante de Workers AI — nunca delante de Claude, camino que D-035 quitó. El tope por perfil y día NO depende de él: vive en el Durable Object (plan F6 §5.1, enmienda D-015). Zero Data Retention en producción: conteos y costos, jamás prompts — el prompt lleva lo que respondió un menor. Caché apagada | `AI_GATEWAY_ID` (var; sin ella se llama sin gateway) |
| `math-challenge-attempts-ae` | Analytics Engine dataset | Per-attempt telemetry (high-cardinality, high-volume) | Telemetría por intento (alta cardinalidad, alto volumen) | `ATTEMPTS_AE` |
| `math-challenge-vitals-ae` | Analytics Engine dataset | Field Core Web Vitals (LCP/CLS/INP/TTFB/FCP); never written from a child surface | Core Web Vitals de campo; jamás se escribe desde una superficie de niño (D-037) | `VITALS_AE` |
| `math-challenge-funnel-ae` | Analytics Engine dataset | Activation funnel for the ADULT: signup, first child profile, first household device. Never a child — D-037 and red line #2 | Embudo de activación del ADULTO: registro, primer perfil de niño, primer dispositivo de la casa. Nunca un niño — D-037 y línea roja #2 | `FUNNEL_AE` |
| `math-challenge-ratelimiter-do` | Durable Object | Two things, two keys. (a) Rate limiter, one instance per (action, IP). (b) **Since F6 #136, the tutor spend meter** on route `/tutor`, one instance per daily profile pseudonym `pd = HMAC(secret, day‖profile_id)`, storing three integers — calls, settled µ$, reserved µ$ — for seven days. A counter needs read-and-write without a race; KV is eventually consistent and allows one write per second per key. The two halves fail in OPPOSITE directions on purpose: the rate limiter fails open (its absence makes a form slow, not open), the meter fails closed (its absence would make spend unbounded) | Dos cosas, dos llaves. (a) Limitador de tasa, una instancia por (acción, IP). (b) **Desde F6 #136, el medidor de gasto del tutor** en la ruta `/tutor`, una instancia por seudónimo diario del perfil `pd = HMAC(secreto, día‖profile_id)`, con tres enteros —llamadas, µ$ liquidados, µ$ reservados— durante siete días. Las dos mitades fallan en direcciones OPUESTAS a propósito: el limitador falla abierto, el medidor falla cerrado | `RATE_LIMITER` |
| `math-challenge-tutor-usage-ae` | Analytics Engine dataset | Tutor usage/cost telemetry indexed `banda\|locale\|modelo`. **NEVER per child, not even hashed** — this row used to say "per-child, per-model", which broke red line #2 and was unfixable after the fact: Analytics Engine keeps three months and has no DELETE (`mc-32` risk #7). The per-profile counter lives in the Durable Object for seven days and IS deleted | Telemetría de uso/costo del tutor indexada `banda\|locale\|modelo`. **JAMÁS por niño, ni siquiera hasheado** — este renglón decía «per-child, per-model», que cruzaba la línea roja #2 y además no tenía arreglo posterior: Analytics Engine retiene tres meses y no tiene DELETE. El contador por perfil vive en el Durable Object siete días y ése sí se borra | `TUTOR_AE` |
| `kilowatto` ⚠️ sin prefijo, ver D-054 | Turnstile widget | Bot defense on signup forms. REUSED: a Turnstile widget belongs to a hostname list, not to a project, and math.kilowatto.com lives inside kilowatto.com | Defensa contra bots en el formulario de registro. REUSADO: un widget de Turnstile pertenece a una lista de hostnames, no a un proyecto, y math.kilowatto.com vive dentro de kilowatto.com | `TURNSTILE_SITE_KEY` (público, var) · `TURNSTILE_SECRET_KEY` (secreto) |
| `math-challenge-web-analytics` | Web Analytics site | Privacy-first RUM for the PWA | RUM respetuoso de la privacidad para la PWA | (JS snippet, no binding) |
| `math-challenge-secrets` | Secrets Store | Third-party credentials. **No longer `ANTHROPIC_API_KEY`**: D-035 removed that path and the `@anthropic-ai/sdk` package was uninstalled. Current tenant of this row: `TUTOR_PD_SECRET`, the HMAC salt for the tutor's daily per-profile pseudonym. Without it the live path does not run at all — no `pd` means no per-profile counter, and a live path without a cap is not switched on | Credenciales de terceros. **Ya no `ANTHROPIC_API_KEY`**: D-035 quitó ese camino y el paquete `@anthropic-ai/sdk` se desinstaló. Inquilino actual: `TUTOR_PD_SECRET`, la sal del HMAC del seudónimo diario del perfil. Sin él el camino en vivo no corre: sin `pd` no hay contador por perfil, y un camino en vivo sin tope no se enciende | vía `wrangler secret put` |
| `math-challenge-league-cycle-workflow` | Workflow | Weekly close of every expired league cohort (F7 #241): promotion/demotion computed over ACTIVE members only (D-056 figures), next-week placement with points reset, and silent archiving after 8 consecutive inactive weeks. Retries are idempotent: one atomic D1 batch per cohort, a guard that skips already-closed cohorts, and deterministic ids with INSERT OR IGNORE. Lives in `math-challenge-ingest`, triggered by cron `10 0 * * MON` | Cierre semanal de cada cohorte de liga vencida (F7 #241): ascenso/descenso calculado SOLO sobre activos (las cifras de D-056), colocación en la semana siguiente con los puntos a cero, y archivado en silencio a las 8 semanas seguidas sin actividad. Reintentos idempotentes: un batch atómico de D1 por cohorte, guardia que salta cohortes ya cerradas, e ids deterministas con INSERT OR IGNORE. Vive en `math-challenge-ingest`, disparado por el cron `10 0 * * MON` | `LEAGUE_CYCLE_WORKFLOW` |
| `math-challenge-missions-do` | Durable Object class (SQLite) | Per-child daily missions state (F7 #224): the day's assigned missions, progress and XP awarded — TODAY only, replaced when the household's local day rolls over. Derived state, never raw attempts. Owns the day's transitions; `mission_daily_summary` in D1 is its read rollup, written by the same code path | Estado de las misiones diarias por niño (F7 #224): las misiones del día, su progreso y el XP otorgado — solo HOY, reemplazado al cambiar el día local del hogar. Estado derivado, jamás intentos crudos. Es el dueño de las transiciones del día; `mission_daily_summary` en D1 es su rollup de lectura, escrito por el mismo camino | `MISSIONS_DO` |


## Bitácora de creación / Creation log

| Fecha | Objeto | ID real | Quién | Nota |
|-------|--------|---------|-------|------|
| 2026-07-31 | `math-challenge-db` (D1) | `25276cac-2d48-4771-87c1-f58bc8722b4e` | Esteban | Región **WNAM**. Migraciones 0001 y 0002 aplicadas en local y remoto; 10 tablas. Binding `DB`, no el `math_challenge_db` que sugiere wrangler |
| 2026-07-31 | `math-challenge-session-kv` (KV) | `c7157f96cd7d478ca8bd0190ef396239` | Esteban | Binding `SESSION_KV`. **Solo tokens efímeros** hasta verificar residencia de KV con Cloudflare |
| 2026-07-31 | `math-challenge-config-kv` (KV) | `76bfad78247544bbb8fbd447a06ad933` | Esteban | Binding `CONFIG_KV` |
| 2026-07-31 | `math-challenge-media` (R2) | *(el nombre es el id)* | Esteban | Binding `MEDIA_BUCKET`. Arte de la Sabana, imágenes y audio |
| 2026-07-31 | `math-challenge-exports` (R2) | *(el nombre es el id)* | Esteban | Binding `EXPORTS_BUCKET`. Archivo frío y exportaciones COPPA/GDPR |
| 2026-08-01 | `math-challenge-learner-do` (Durable Object, clase `Aprendiz`) | *(la clase es el id)* | Claude | Binding `LEARNER_DO`, migración de DO `v2` con `new_sqlite_classes`. **Un objeto por niño** (`idFromName(child_profile_id)`) — un DO global topa en 500-1.000 req/s (`mc-32` riesgo #2) y, sobre todo, hace imposible que borrar el perfil sea `deleteAll()`. Guarda **estado derivado**: estimaciones, contadores y fechas; jamás el intento crudo, que va a `math-challenge-attempts-ae`. Tres sitios hay que tocar para añadir una clase de DO y olvidar cualquiera rompe distinto: `worker.ts` (export con nombre), `astro.config.mjs` (`namedExports`) y `wrangler.jsonc` (binding + `migrations`) |
| 2026-08-02 | `mail.kilowatto.com` ⚠️ sin prefijo — es un DOMINIO, no un objeto de la cuenta | Cloudflare Email Service (Email Sending) | Esteban | Binding `EMAIL`, `"remote": true`. Envía a CUALQUIER destinatario, a diferencia de Email Routing —que sigue en `enabled: false`— que solo recibiría. **Reputación «At Risk» desde el día uno**: 10 envíos de prueba con 1 rebote dan 10% contra un umbral de riesgo del 5%, y Cloudflare pausa el envío de la cuenta si no baja. Por eso el reseteo de contraseña solo puede escribir a direcciones que ya estén en `users` (issue #313). Cuota 1000/día. DNS: MX de rebotes, SPF, DKIM y DMARC en `_dmarc` |

| 2026-08-02 | Binding `AI` (Workers AI) + dataset `math-challenge-tutor-usage-ae` | *(ninguno: Workers AI no crea objeto de cuenta; el dataset aparece la primera vez que un Worker le escribe)* | Claude | F6 #136. **Lo que este renglón NO significa:** no se creó el AI Gateway `math-challenge-tutor-gateway`, así que la red de seguridad en dólares **no está activa**. No hace falta para que el tope funcione — el tope por perfil y día lo hace cumplir el Durable Object (plan F6 §5.1, enmienda D-015)— pero sí para el segundo cinturón. Y falta `TUTOR_PD_SECRET` por `wrangler secret put`: **sin él no se llama al modelo en absoluto**, así que el estado de hoy es «camino en vivo apagado por falta de secreto», que es el estado seguro. Tres cosas hay que hacer para encenderlo: crear el gateway, poner el secreto, y marcar el plan de una cuenta como `familia` en `CONFIG_KV` |

| 2026-08-03 | `math-challenge-league-do` (Durable Object, clase `Liga`) + migración D1 `0012_ligas_tablero_duelo.sql` | *(la clase es el id)* | Claude | F7 #242. Binding `LEAGUE_DO`, migración de DO `v3` con `new_sqlite_classes` — etiqueta nueva, nunca una edición de la `v2`: una migración de DO ya desplegada es inmutable y editarla deja el despliegue rechazado con «migration tag mismatch». **Un objeto por cohorte** (`idFromName(cohort_id)`), no uno global: un DO global topa en 500-1.000 req/s (`mc-32` riesgo #2) y, sobre todo, hace imposible que cerrar una liga sea `deleteAll()`. Guarda **estado derivado** —puntos de la semana, días activos, alias y avatar para poder difundir sin ir a D1—; jamás el intento crudo, que va a `math-challenge-attempts-ae`. **Ninguna señal de presencia**: no hay contador de sockets, no hay `last_seen`, y la difusión manda la tabla entera y nunca «fulano acaba de jugar» (condición 2 de D-081). Los tres sitios de siempre: `worker.ts` (export con nombre), `astro.config.mjs` (`namedExports`) y `wrangler.jsonc` (binding + `migrations`) |
| 2026-08-02 | Migración `0011_screen_time_daily_usage.sql` sobre `math-challenge-db` (D1) | *(tabla `screen_time_daily_usage`, no un objeto de la cuenta)* | Claude | F8 #267. **Cero recursos nuevos de Cloudflare**: el renglón existe porque la migración se commiteó, no porque se haya creado nada. Una tabla, solo AGREGA, `PRIMARY KEY (child_profile_id, local_date)` y `ON DELETE CASCADE` sobre `child_profiles`. Es el CONSUMO del día —cuántos minutos lleva jugados el niño hoy— que `screen_time_settings` (la configuración) nunca guardó, y sin el cual el aviso de los 5 minutos y el corte diario de D-016 no se pueden calcular. Es un rollup por niño y por día, el mismo patrón que `score_totals`: **no** es lo que `mc-32` riesgo #1 prohíbe, que son intentos crudos y siguen yendo solo a `math-challenge-attempts-ae`. Se descartó un `math-challenge-screentime-do` por niño: sumar minutos no tiene la exigencia de consistencia serializada que justifica un DO en F4, y un objeto más que inventariar sin problema de latencia que resolver es costo sin beneficio. **Lo que este renglón NO significa:** la migración todavía no se ha aplicado a `math-challenge-db` ni a `math-challenge-db-eu` — sin `wrangler d1 migrations apply` la tabla no existe en ningún ambiente, y el motor que la lee (`packages/motor/src/limite-pantalla.ts`) es puro y no la toca todavía. ~~Y **la numeración salta de 0008 a 0011 a propósito**: 0009 y 0010 están reservadas a F7 misiones y a F7 ligas, que se construyen en paralelo; `audits/migration-safety.mjs` avisa del hueco hasta que esas dos ramas mergeen, y tiene razón en avisar~~ **Corrección (2026-08-03): esta nota de numeración quedó obsoleta — `0009` (misiones), `0010` (mapa) y `0012` (ligas) ya están en `main`; la serie 0001..0012 corre sin huecos. Y sigue sin aplicarse a ninguna base: el registro `d1_migrations` de producción está desincronizado (0003–0006 aplicadas a mano y no registradas, `docs/dudas.md` §23.3), así que aplicarla exige primero la reparación escrita y aún no ejecutada.** |
| 2026-08-03 | `math-challenge-league-cycle-workflow` (Workflow, clase `CicloLigaSemanal`) — **DECLARADO EN CÓDIGO, TODAVÍA NO CREADO NI DESPLEGADO** | *(sin instancias hasta el primer despliegue)* | Claude | F7 #241. Vive en `math-challenge-ingest` (`apps/ingest/src/ciclo-liga.ts` + `ciclo-liga-workflow.ts`), con el cron `10 0 * * MON` —lunes 00:10 UTC: la semana de liga es UTC y cierra al empezar el lunes, y el margen de diez minutos es para que un cron adelantado no cierre la semana en curso—. Se eligió `ingest` y no `web`: el cierre es trabajo por lotes sin ruta pública (como todo lo de ese Worker), solo necesita el binding `DB`, y no tiene por qué pagar el despliegue pesado del Astro de web. **Lo que este renglón NO significa:** el cron no corre hasta que alguien despliegue `math-challenge-ingest` — no hay CI y el despliegue es manual, así que la primera semana que cerrará de verdad es la primera que siga a ese despliegue. Tampoco borra el estado del Durable Object de la cohorte cerrada: `LEAGUE_DO` vive en `web` y cruzar el binding se dejó fuera a propósito (ver el PR) |
| 2026-08-03 | Migración `0014_push_recordatorio_padre.sql` sobre `math-challenge-db` (D1) + cron `*/30 * * * *` en `math-challenge-web` — **DECLARADO EN CÓDIGO, TODAVÍA NO APLICADO NI DESPLEGADO** | *(tablas `push_subscription` y `push_recordatorio`, no un objeto de la cuenta)* | Claude | F7 #207, D-105. **Cero recursos nuevos de Cloudflare**: dos tablas solo-agregar en la base ya inventariada. El cron del recordatorio vive en `math-challenge-web` y no en `ingest` —al revés que el ciclo de liga— porque la suscripción, el copy y la clave pública VAPID son de la superficie del padre, que es web, y duplicar VAPID en dos Workers sería un secreto más que rotar. **Lo que este renglón NO significa:** (1) la migración no está aplicada en ningún ambiente — corre con `wrangler d1 migrations apply`, y el registro `d1_migrations` de producción sigue desincronizado (ver el renglón de la 0011); (2) el cron no envía NADA hasta que existan `VAPID_PRIVATE_KEY` (`wrangler secret put`) y `VAPID_PUBLIC_KEY` (var) — sin ellas el ciclo degrada en silencio, que es el estado seguro por defecto; (3) el envío real no está verificado en un aparato de verdad: falta la prueba con una suscripción real, en especial en iOS, donde Web Push exige 16.4+ con la PWA INSTALADA (mc-19 §2.1, puerta absoluta) |
| 2026-08-03 | Migración `0015_cosmeticos_kinder_v1.sql` sobre `math-challenge-db` (D1) — **DECLARADA EN CÓDIGO, TODAVÍA NO APLICADA** | *(tablas `cosmetic_catalog`, `cosmetic_unlock_rules`, `child_cosmetics_unlocked`, no un objeto de la cuenta)* | Claude | F7 #255 (paraguas #252). **Cero recursos nuevos de Cloudflare**: tres tablas solo-agregar en la base ya inventariada, y el catálogo v1 de KINDER sembrado en la misma migración (23 filas, deterministas, sin precio ni azar — línea roja #5, D-014). El ESQUEMA se crea aquí y no antes porque la issue de esquema (#253) se cerró como «en main» sin que sus `CREATE TABLE` aterrizaran nunca en `migrations/` — detectado con `grep -rn "cosmetic_catalog" migrations/` sobre main y anotado dentro del propio SQL. **Lo que este renglón NO significa:** (1) la migración no está aplicada en ningún ambiente — corre con `wrangler d1 migrations apply`, y el registro `d1_migrations` de producción sigue desincronizado (ver el renglón de la 0011); (2) 6 de las 23 filas se publican SIN arte (las habilidades K05, K06, K08, K09, K13 y K14 esperan a F5; K14 está BLOQUEADA en el plan de F5) — son filas de catálogo con `arte_*_url` en NULL, no piezas jugables todavía; (3) ninguna pantalla lee el catálogo todavía: la superficie es #284 (F8), y la condición de desbloqueo es texto solo para el panel del padre (mc-43 §8) |
| 2026-08-03 | Migración `0016_banco_items_primaria.sql` sobre `math-challenge-db` (D1) — **DECLARADA EN CÓDIGO, TODAVÍA NO APLICADA** | *(tabla `item_bank` + índice `idx_item_bank_lectura`, no un objeto de la cuenta)* | Claude | F5c #351 (paraguas #350), D-072. **Cero recursos nuevos de Cloudflare**: una tabla solo-agregar y su índice en la base ya inventariada. La siembra la genera `scripts/sembrar-banco-primaria.mjs` (INSERT OR IGNORE desde las plantillas de `banco-primaria.ts`) y **tampoco está aplicada**: corre con `wrangler d1 execute`. **Lo que este renglón NO significa:** (1) la migración no está aplicada en ningún ambiente — corre con `wrangler d1 migrations apply`, y el registro `d1_migrations` de producción sigue desincronizado (ver el renglón de la 0011); (2) hasta que la siembra corra en un ambiente, `/api/jugar` sirve a los adultos el banco de KINDER como respaldo (caída deliberada, línea roja #4 en espíritu: nunca se niega el juego por infraestructura); (3) KINDER no migra a esta tabla hoy — es la deuda consciente que la propia D-072 nombra |
| 2026-08-03 | `math-challenge-missions-do` (Durable Object, clase `Misiones`) — **DECLARADO EN CÓDIGO, TODAVÍA NO CREADO NI DESPLEGADO** | *(la clase es el id; sin instancias hasta el primer despliegue)* | Claude | F7 #224. Binding `MISSIONS_DO`, migración de DO `v4` con `new_sqlite_classes` — etiqueta nueva, nunca una edición de una ya desplegada («migration tag mismatch»). **Un objeto por niño** (`idFromName(child_profile_id)`; el adulto aprendiz abre el suyo con su `user_id`, que la 0009 ya contempla), separado de `math-challenge-learner-do` (F4) por el argumento estructural de D-027: dominios distintos, dueños distintos, y un bug en uno no puede tocar al otro. Guarda **estado derivado del DÍA** —misiones asignadas, progreso, XP otorgado, habilidades distintas de hoy— reemplazado entero al cambiar el día local del hogar; jamás el intento crudo, que va a `math-challenge-attempts-ae` (mc-32 riesgo #1). **Forma elegida: el DO es el dueño de las transiciones del día y `mission_daily_summary` (0009) es su rollup de lectura** — un solo camino (`lib/misiones-dia.ts`) escribe el primario y su espejo en la misma petición, así que no hay dos escritores del mismo hecho, y la serialización del objeto cierra la carrera que podía pagar el bono del día dos veces. El archivo es `missions-do.ts` y no `misiones-do.ts` porque `audits/misiones-sin-do-ajeno.mjs` vigila los basenames `mision*` contra objetos AJENOS y este es el objeto del propio subsistema. Los tres sitios de siempre: `worker.ts` (export con nombre), `astro.config.mjs` (`namedExports`) y `wrangler.jsonc` (binding + `migrations`). **Lo que este renglón NO significa:** la clase no existe en la cuenta hasta que el orquestador despliegue `math-challenge-web` — sin ese despliegue el binding `MISSIONS_DO` no responde, `avanzarEnMisiones` falla ABIERTO (se pierde un contador, no el juego) y `mission_daily_summary` deja de recibir filas nuevas: las misiones quedan apagadas en silencio hasta el despliegue |

| 2026-08-03 | `math-challenge-classroom-do` (Durable Object, clase `Salon`) + migración D1 `0017_grupos_infantiles.sql` — **DECLARADO EN CÓDIGO, TODAVÍA NO CREADO NI APLICADO** | *(la clase es el id; sin instancias hasta el primer despliegue)* | Claude | F9 #379, D-098. Binding `CLASSROOM_DO`, migración de DO `v5` con `new_sqlite_classes` — etiqueta nueva: la `v4` quedó para `Misiones` (ya desplegada, inmutable). **Un objeto por grupo** (`idFromName(group_id)`), no uno global (mc-32 riesgo #2) y, sobre todo, para que borrar el grupo sea `deleteAll()`. Guarda **estado derivado** — puntos y racha YA calculados, alias y avatar para difundir sin ir a D1, la banda solo para calcular la posición visible (se almacena y NO se proyecta) —; jamás el intento crudo, que va a `math-challenge-attempts-ae`. La vista ordenada excluye a los miembros con `leaderboard_opt_in = 0` (D-087) y el filtro vive DENTRO del objeto, no en cada lector. Los tres sitios de siempre: `worker.ts` (export con nombre), `astro.config.mjs` (`namedExports`) y `wrangler.jsonc` (binding + `migrations`). **Lo que este renglón NO significa:** (1) la migración no está aplicada en ningún ambiente — corre con `wrangler d1 migrations apply`, y el registro `d1_migrations` de producción sigue desincronizado (ver el renglón de la 0011); (2) ninguna ruta llama al objeto todavía — las rutas/pantallas de F9 (tarjeta de identidad, roster, aprobación, reporte, bitácora) son de las issues #381-#386 y NO viajan en este PR; (3) `f9_enabled_<locale>` en `CONFIG_KV` (issue #387) no existe aún: sin bandera y sin rutas, F9 está apagado por construcción en los 7 locales |

| 2026-08-04 | Llaves de `math-challenge-config-kv` (KV) `f9_enabled_<locale>` ×7 + `f9_dominios_escuela`, y prefijo `escuela/` en `math-challenge-media` (R2) — **DECLARADAS EN CÓDIGO, NO ESCRITAS** | *(llaves dentro del namespace `76bfad78247544bbb8fbd447a06ad933` y objetos dentro del bucket ya inventariados: cero recursos nuevos de la cuenta)* | Claude | F9 #387 (bandera) y #381 (documento de escuela). (1) `f9_enabled_<locale>` vale `"1"` en los mercados con la superficie de grupos encendida (`en`, `es-MX`, `es-ES`, `pt-BR` al lanzar) y **no existe** en `fr-FR`/`pt-PT`/`de-DE`: la llave ausente es el estado apagado, leído en el servidor por `lib/grupo-flag.ts` en cada ruta de `/app/grupos/` y `/api/grupos/*`. La condición que enciende los tres apagados es una **confirmación legal escrita** de GDPR Art. 8 / Children's Code, no una fecha (criterio de #387). Se escriben con `wrangler kv key put --binding CONFIG_KV f9_enabled_en 1` (y así por locale). (2) `f9_dominios_escuela`: JSON con la lista de dominios institucionales reconocidos para el atajo de D-086 — vacía/ausente = ningún dominio reconocido, el atajo se enciende escribiendo la lista con datos, nunca por una constante inventada (cabecera de la 0017). (3) El prefijo `escuela/<school_id>/documento` del bucket guarda los documentos de verificación de escuela (AVIF/PDF, ≤5 MB) con el `user_id` del registrante en `customMetadata.registrante` — la tabla `school` no tiene columna para él y el revisor lo necesita para dar de alta al maestro. **Lo que este renglón NO significa:** ninguna llave ni objeto fue escrito en la cuenta — el PR declara el mecanismo y la superficie queda apagada hasta que el dueño escriba las banderas |

| 2026-08-04 | Migración `0018_child_diagnostic_notes.sql` sobre `math-challenge-db` (D1) — **DECLARADA EN CÓDIGO, TODAVÍA NO APLICADA** | *(tabla `child_diagnostic_notes`, no un objeto de la cuenta)* | Claude | F8 #278 (paraguas #277), D-020, `f6-larry-profe.md` §2.3. **Cero recursos nuevos de Cloudflare**: una tabla solo-agregar en la base ya inventariada. Es la única tabla que F8 · Panel con diagnóstico posee: todo lo demás que el panel muestra se lee de las tablas de F2/F4/F7/F8-límite, y la tendencia de minutos vive en `screen_time_daily_usage` (0011), no en una tabla propia — la `screen_time_daily` del primer borrador de #278 se descartó en la reconciliación. Los ESCRITORES (F4 para `PATRON_INUSUAL_PARA_EDAD`, F6 para `HABILIDAD_PAUSADA_LATERAL`) no existen todavía: la migración fija el contrato. **Lo que este renglón NO significa:** (1) la migración no está aplicada en ningún ambiente — corre con `wrangler d1 migrations apply`, y el registro `d1_migrations` de producción sigue desincronizado (ver el renglón de la 0011); (2) ninguna fila puede existir hoy: sin escritores, la sección de notas del panel se renderiza vacía hasta que F4/F6 aterricen; (3) la numeración NO lleva `migration-safety-reserva` a propósito — la rama corre contigua (…0017, 0018) y una reserva declarada para la 0019 (frente de reportes, en paralelo) apagaría la sonda de hueco de `audits/pruebas-auditores.mjs`, reapuntada a `0020_prueba_hueco.sql` en el mismo PR |

| 2026-08-05 | Migración `0022_clubs_adultos.sql` sobre `math-challenge-db` (D1) — **DECLARADA EN CÓDIGO, TODAVÍA NO APLICADA** | *(tablas `adult_club`, `adult_club_membership`, `club_challenge`, resultados y prendas moderadas; cero objetos nuevos de la cuenta)* | Claude | F10 #412, D-043, D-117–D-121. Tope estructural de 20 miembros, membresía XOR con aprobación parental para adolescentes, retos con máximo 72 horas y resultados aislados del tablero global. No contiene celda de perdedor, penalización ni forfeit. Las rutas de club, moderación y apelaciones siguen pendientes; la reparación del registro `d1_migrations` es prerrequisito para aplicarla. |

> **Regla:** quien crea un recurso de Cloudflare escribe su renglón aquí en el
> mismo PR (`CLAUDE.md` § Cloudflare).

| 2026-08-04 | Migración `0019_reportes_correo.sql` sobre `math-challenge-db` (D1) + colas `math-challenge-reports-queue` y `math-challenge-reports-dlq` + cron `0 * * * *` en `math-challenge-web` — **DECLARADO EN CÓDIGO, TODAVÍA NO APLICADO, CREADO NI DESPLEGADO** | *(tablas `parent_report_settings` y `child_report_state`; las colas son objetos de la cuenta, sin crear)* | Claude | F8 #287/#289, D-026, D-057. Las dos tablas solo-agregar van en la base ya inventariada (cero objetos nuevos por ese lado); las COLAS sí son objetos nuevos y **se crean a mano antes del primer despliegue con este config**: `npx wrangler queues create math-challenge-reports-queue` y `npx wrangler queues create math-challenge-reports-dlq`. El cron y el consumidor de cola viven en `math-challenge-web` y NO en un Worker `math-challenge-reports` nuevo (el issue #289 lo proponía): la preferencia, la baja y el binding `EMAIL` son de la superficie del padre, que es este Worker, y un tercer objeto sería otra configuración que desincronizar sin problema de latencia que resolver — mismo criterio que el cron del push (#207). Lo que el issue pedía separar, el presupuesto de CPU del cron, se separa con la cola: el `scheduled()` solo enumera y encola. El remitente es `reportes@mail.kilowatto.com` sobre el dominio ya dado de alta (#313), reputación «At Risk»: solo se escribe a direcciones VERIFICADAS (`email_verified = 1`) y una dirección suprimida apaga la cadencia del hogar sin reintento. **Lo que este renglón NO significa:** (1) la migración no está aplicada en ningún ambiente — corre con `wrangler d1 migrations apply`, y el registro `d1_migrations` de producción sigue desincronizado (ver el renglón de la 0011); (2) las colas no existen en la cuenta hasta que el orquestador las cree — sin ellas el ciclo degrada en silencio (enumera, no encola, no rompe), el estado seguro por defecto; (3) ningún correo real se ha enviado: falta la prueba de humo contra un buzón de prueba, tras crear las colas y aplicar la migración |
| 2026-08-04 | Migración `0020_cosmeticos_arte_restante.sql` sobre `math-challenge-db` (D1) — **DECLARADA EN CÓDIGO, TODAVÍA NO APLICADA** | *(solo UPDATE de datos sobre `cosmetic_catalog`; el arte son archivos estáticos en `apps/web/public/cosmeticos/`, no un objeto de la cuenta)* | Claude | F7 #255 (paraguas #252). **Cero recursos nuevos de Cloudflare**: seis UPDATE con `AND arte_avif_url IS NULL` escriben las URL del arte de las 6 piezas que la 0015 publicó sin él. El arte se generó con `scripts/gen-cosmeticos.mjs` y cada imagen se MIRÓ antes de aceptarse (tres piezas necesitaron regeneración: dos sacaron muestrario de color, una salió con formas inventadas). **Lo que este renglón NO significa:** (1) la migración no está aplicada en ningún ambiente — la aplica el orquestador al integrar, con `wrangler d1 migrations apply`, y el registro `d1_migrations` de producción sigue desincronizado (ver el renglón de la 0011); (2) las 15 filas con arte, las reglas de `cosmetic_unlock_rules` y los dos marcos de hito no los toca — el seguro del WHERE lo hace estructural; (3) ninguna pantalla lee el catálogo todavía: la superficie sigue siendo #284 (F8), y la condición de desbloqueo es texto solo para el panel del padre (mc-43 §8) |
| 2026-08-04 | Migración `0021_alias_unico_por_padre.sql` sobre `math-challenge-db` (D1) — **DECLARADA EN CÓDIGO, TODAVÍA NO APLICADA** | *(índice único y UPDATE de deduplicación sobre `child_profiles`; cero objetos de la cuenta)* | Claude | F2 #259, D-003. **Cero recursos nuevos de Cloudflare**: un UPDATE de deduplicación (explícito, determinista, idempotente — conserva el perfil más antiguo de cada grupo (padre, alias), renombra a los demás con un sufijo `-XXXXXX` derivado de su propio id, NUNCA borra) y la reafirmación con `IF NOT EXISTS` de `idx_alias_por_padre`, UNIQUE (parent_user_id, alias) WHERE deleted_at IS NULL, el índice que la 0006 ya creó pero cuya aplicación manual a producción este repo no puede verificar. Por padre y no global por decisión del orquestador (el issue pedía global por el tablero; queda anotado en la propia migración). **Lo que este renglón NO significa:** (1) la migración no está aplicada en ningún ambiente — la aplica el orquestador al integrar, con `wrangler d1 migrations apply`, y el registro `d1_migrations` de producción sigue desincronizado (ver el renglón de la 0011); (2) no resuelve el choque de alias ENTRE familias en el tablero global — eso queda para la presentación del tablero si alguna vez importa; (3) no toca `users.alias` (el velo del adulto de la 0012), cuya unicidad es otro problema |


> **Regla:** quien crea un recurso de Cloudflare escribe su renglón aquí en el
> mismo PR (`CLAUDE.md` § Cloudflare).

### Ajustes de zona (no son objetos, pero se rompen igual)

Estos no aparecen en el inventario porque no son objetos de la cuenta, sino
ajustes de la zona `kilowatto.com`. Se anotan porque nada en el repositorio los
declara: si alguien los apaga, el código no se entera.

| Ajuste | Estado | Evidencia | Fecha |
|--------|--------|-----------|-------|
| HTTP/2 | activo | — | 2026-07-31 |
| HTTP/2 to Origin | activo | — | 2026-07-31 |
| HTTP/3 (con QUIC) | activo | `alt-svc: h3=":443"` | 2026-07-31 |
| **0-RTT Connection Resumption** | **activo** | ticket TLS 1.3 con `Max Early Data: 14336` | 2026-07-31 |
| Enhanced HTTP/2 Prioritization | no disponible | requiere plan Pro; la zona es Free | — |

`audits/live.mjs` verifica HTTP/3 y 0-RTT en cada corrida, así que un apagón
accidental se detecta al desplegar en vez de meses después. Están en
**Speed → Optimization → Protocol Optimization**.

### Lo que quedó decidido sin decidirse: la jurisdicción

`math-challenge-db` se creó en **WNAM** (Norteamérica oeste). Eso importa más de
lo que parece: `mc-25` documenta que D1 tiene ajuste de jurisdicción por base de
datos desde noviembre de 2025, y que —igual que en R2— **se fija al crear y no se
puede cambiar después**.

Es decir, esta base **no puede convertirse en una base de jurisdicción europea**.
Si en algún momento hay que fijar residencia de datos para menores de la UE o del
Reino Unido —que es la implicación 11 de `mc-25`—, hace falta una **segunda base
con jurisdicción `eu`**, y la decisión de a cuál va cada familia se toma **en el
momento del registro**, no después.

No es un error: para el MVP, con el mercado inicial en LatAm y EE.UU., WNAM es lo
correcto. Pero es una puerta que se cerró al crear el objeto, y conviene que esté
escrito antes de que alguien la busque y no la encuentre.

**Pendiente asociado:** `mc-25` señala que la historia de residencia de Workers KV
**no se pudo confirmar**; hay que verificarla directamente con Cloudflare antes de
guardar datos personales de menores de la UE o el Reino Unido en `SESSION_KV` o
`CONFIG_KV`.

### `math-challenge-db-eu`: existe, y este proyecto NO la usa

En la cuenta de Cloudflare hay una segunda base D1 llamada `math-challenge-db-eu`.
Comprobado el 2026-08-03, no supuesto: **no aparece en ninguna configuración de
wrangler del repositorio** — ningún `wrangler.jsonc` la referencia, ningún
binding apunta a ella, y ninguna migración se le ha aplicado jamás por la vía
normal. No es la «base europea» que la sección de jurisdicción de arriba
contempla: esa segunda base, cuando haga falta, se creará con jurisdicción `eu`
explícita y con la decisión de enrutado en el registro.

**Decisión del dueño (2026-08-03): se documenta y se deja quieta; no se borra.**
Borrarla es irreversible y exigiría primero confirmar que está vacía; documentarla
cuesta este renglón y cierra la pregunta «¿esto se puede borrar?» durante un año.
Si alguien la necesita para la jurisdicción europea, la decisión es crear una base
NUEVA con ajuste de jurisdicción — esta no lo tiene fijado por escrito en ningún
lado del proyecto.

## Riesgo conocido

El precio por escritura de Analytics Engine **no pudo confirmarse** contra la
documentación actual de Cloudflare durante la investigación. Como todo el diseño
de telemetría descansa en ese servicio, hay que confirmarlo antes de
comprometerse — está registrado como riesgo #13 en `mc-32`.
